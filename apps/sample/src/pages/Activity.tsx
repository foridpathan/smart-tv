import { Screen } from '@smart-tv/ui';

const Activity = () => {
    return (
        <Screen>
            <div className="text-white text-5xl font-bold flex items-center justify-center h-full">Activity Page</div>
        </Screen>
    );
};

export default Activity;