import { Screen } from '@smart-tv/ui';

const Favorites = () => {
    return (
        <Screen>
            <div className="text-white text-5xl font-bold flex items-center justify-center h-full">Favorites Page</div>
        </Screen>
    );
};

export default Favorites;