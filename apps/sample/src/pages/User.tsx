import { Screen } from '@smart-tv/ui';

const User = () => {
    return (
        <Screen>
            <div className="text-white text-5xl font-bold flex items-center justify-center h-full">User Page</div>
        </Screen>
    );
};

export default User;