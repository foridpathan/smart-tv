import { Screen } from '@smart-tv/ui';

const Search = () => {
    return (
        <Screen>
            <div className="text-white text-5xl font-bold flex items-center justify-center h-full">Search Page</div>
        </Screen>
    );
};

export default Search;