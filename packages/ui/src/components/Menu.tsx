import React from 'react';

// TV menu component
export const Menu = ({ children }: { children: React.ReactNode }) => {
  // TODO: Add menu navigation logic for TV
  return <div className="tv-menu">{children}</div>;
};
