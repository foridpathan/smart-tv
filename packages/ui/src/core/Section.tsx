import React from 'react';

// Section for grouping TV UI elements
export const Section = ({ children }: { children: React.ReactNode }) => {
  // TODO: Add section-level focus/scroll logic
  return <div>{children}</div>;
};
