import React from 'react';

// TV column layout
export const Column = ({ children }: { children: React.ReactNode }) => {
  // TODO: Add column navigation for TV
  return <div className="tv-column">{children}</div>;
};
