import React from 'react';

// TV grid layout
export const Grid = ({ children }: { children: React.ReactNode }) => {
  // TODO: Add grid navigation for TV
  return <div className="tv-grid">{children}</div>;
};
