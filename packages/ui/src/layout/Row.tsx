import React from 'react';

// TV row layout
export const Row = ({ children }: { children: React.ReactNode }) => {
  // TODO: Add row navigation for TV
  return <div className="tv-row">{children}</div>;
};
