import React from 'react';

// Grid layout for focusable TV elements
export const FocusGrid = ({ children }: { children: React.ReactNode }) => {
  // TODO: Implement grid navigation logic
  return <div className="focus-grid">{children}</div>;
};
