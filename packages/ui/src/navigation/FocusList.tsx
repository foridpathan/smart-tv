import React from 'react';

// List layout for focusable TV elements
export const FocusList = ({ children }: { children: React.ReactNode }) => {
  // TODO: Implement list navigation logic
  return <ul className="focus-list">{children}</ul>;
};
