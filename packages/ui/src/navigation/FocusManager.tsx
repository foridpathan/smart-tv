import React from 'react';

// Manages focus for TV navigation
export const FocusManager = ({ children }: { children: React.ReactNode }) => {
  // TODO: Implement focus management logic for remote navigation
  return <>{children}</>;
};
